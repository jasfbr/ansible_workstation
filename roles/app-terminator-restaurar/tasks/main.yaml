---

- name: Restaura o diretório .config/terminator
  ansible.builtin.copy:
    src: '{{path_to_backup}}/home/{{user.login}}/.config/terminator/'
    dest: /home/{{user.login}}/.config/terminator/
    local_follow: false
    mode: preserve
    owner: '{{user.login}}'
    group: '{{user.group}}'

...