---

- name: restaura o diretório .config/spotify
  ansible.builtin.copy:
    src: '{{path_to_backup}}/home/{{user.login}}/.config/spotify/'
    dest: '/home/{{user.login}}/.config/spotify/'
    local_follow: false
    mode: preserve
    owner: '{{user.login}}'
    group: '{{user.group}}'

...